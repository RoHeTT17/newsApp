import 'package:flutter/material.dart';

/*final theme = ThemeData.dark();

final miTema = theme.copyWith(
    colorScheme: theme.colorScheme.copyWith(secondary: Colors.red)
);*/

final miTema = ThemeData.dark().copyWith(
  colorScheme: ThemeData.dark().colorScheme.copyWith(secondary: Colors.teal)
);